# Copy this file to `.streamlit/secrets.toml` on the server (never commit the real one).
# The visualization gates access behind this shared password (anti-crawl, see visualization/_auth.py).
password = "change-me"
